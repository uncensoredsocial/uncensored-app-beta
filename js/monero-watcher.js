// js/monero-watcher.js
// Temporary placeholder so the backend doesn't crash.
// Later you can replace this with a real Monero watcher that talks to your node.

module.exports = function startMoneroWatcher() {
  console.log("Monero watcher placeholder running (no daemon hooked up yet).");
  // Do nothing for now.
};
